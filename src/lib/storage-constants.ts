// Private IM links are deliberately short-lived but long enough to read a
// document without the dossier and acquisition views behaving differently.
export const IM_SIGNED_URL_TTL_SECONDS = 30 * 60;
